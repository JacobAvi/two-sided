<%= form_with(model: trip, local: true) do |form| %>
  <% if trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_address %>
    <%= form.text_field :start_address, id: :start_address_id %>
  </div>

  <div class="field">
    <%= form.label :finish_address %>
    <%= form.text_field :finish_address, id: :address_id %>
  </div>

  <div class="field">
    <%= form.label :avail_seats %>
    <%= form.text_field :avail_seats, id: :trip_avail_seats %>
  </div>

  <div class="field">
    <%= form.hidden_field :driver, id: :trip_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>