<p id="notice"><%= notice %></p>

<p>
  <strong>Start address:</strong>
  <%= @trip.start_address_id %>
  <p><strong>Coordinates:</strong> <%# @trip.start_lat %> <%# @trip.start_long %></p>
</p>

<p>
  <strong>Finish address:</strong>
  <% @trip.finish_address_id %>
    <p><strong>Coordinates:</strong> <%# @trip.finish_lat %> <%# @trip.finish_long %></p>
</p>

<p>
  <strong>Fare:</strong>
  <%= @trip.fare %>
</p>

<p>
  <strong>Avail seats:</strong>
  <%= @trip.avail_seats %>
</p>

<p>
  <strong>User:</strong>
  <%= @trip.driver %>
</p>


<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>

<%= link_to 'Make a request to join this trip!', new_trip_request_path(trip_id: @trip.id) %>